# Math

Inline math: $a^2 + b^2 = c^2$

Block math:

$$
\frac{d}{dx}\left[\int_{a}^{x} f(t)\,dt\right] = f(x)
$$

Inline with symbols: $\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$

Display with environment:

$$
\begin{bmatrix}
1 & 2 \\
3 & 4
\end{bmatrix}
$$
