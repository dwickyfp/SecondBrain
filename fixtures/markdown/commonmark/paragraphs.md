A simple paragraph.

A paragraph with
a hard line break.

A paragraph with trailing whitespace.  
Two trailing spaces create a hard break.

A paragraph with **bold**, *italic*, `code`, and [link](https://example.com).
